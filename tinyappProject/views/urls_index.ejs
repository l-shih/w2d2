<!DOCTYPE html>
<html>
<head>
  <title>TinyApp</title>
</head>
<body>
  <ul>
  <% for(url in urls) { %>
    <li><%= `${url}: ${urls[url]}` %></li>
  <% } %>
  </ul>
  <pre><%#= JSON.stringify(urls[url]) %></pre>
</body>
</html>